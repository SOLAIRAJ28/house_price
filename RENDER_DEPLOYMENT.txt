🚀 RENDER DEPLOYMENT CONFIGURATION
====================================

✅ BUILD & START COMMANDS CONFIGURED

Build Command:
  pip install -r requirements.txt

Start Command:
  python main.py

====================================
📋 RENDER.YAML CONFIGURATION
====================================

File: render.yaml
Location: c:\house_price\render.yaml

Contents:
─────────────────────────────────
services:
  - type: web
    name: house-price-predictor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
─────────────────────────────────

====================================
🔧 WHAT EACH COMMAND DOES
====================================

BUILD COMMAND: pip install -r requirements.txt
  ✓ Runs when you deploy or redeploy
  ✓ Installs all Python dependencies
  ✓ Includes:
    - fastapi==0.104.1
    - uvicorn==0.24.0
    - gradio==4.20.1
    - scikit-learn==1.3.2
    - joblib==1.3.2
    - pydantic==2.5.0

START COMMAND: python main.py
  ✓ Runs after build completes
  ✓ Starts both FastAPI and Gradio
  ✓ FastAPI listens on port 10000
  ✓ Gradio listens on port 7860
  ✓ Render automatically handles port mapping

====================================
🌐 WHAT GETS DEPLOYED
====================================

Your App URLs (after deploy):
  ✓ Main Interface: https://house-price-predictor.onrender.com
  ✓ API Docs: https://house-price-predictor.onrender.com/docs
  ✓ API Endpoint: POST https://house-price-predictor.onrender.com/predict
  ✓ Health Check: GET https://house-price-predictor.onrender.com/health

Files Deployed:
  ✓ main.py (FastAPI + Gradio app)
  ✓ house_model.pkl (trained model)
  ✓ requirements.txt (dependencies)
  ✓ All other supporting files

====================================
📊 DEPLOYMENT PROCESS
====================================

Step 1: GitHub Push
  $ git push -u origin main
  ✓ Your code goes to GitHub

Step 2: Render Detection
  ✓ Render detects new push
  ✓ Triggers automatic build

Step 3: Build Phase (1-2 min)
  $ pip install -r requirements.txt
  ✓ Downloads and installs dependencies

Step 4: Start Phase (30 sec)
  $ python main.py
  ✓ Starts FastAPI server
  ✓ Starts Gradio interface
  ✓ Loads ML model
  ✓ App goes live

Step 5: Ready
  ✓ App accessible at public URL
  ✓ All features working

====================================
🎯 DEPLOYMENT STEPS ON RENDER
====================================

1. Go to https://render.com

2. Click "New +" → "Web Service"

3. Click "Connect GitHub" and authorize

4. Select "house-price-api" repository

5. Fill in the form:
   Name: house-price-predictor
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: python main.py
   Instance Type: Free

6. Click "Deploy"

7. Wait 2-3 minutes

8. Get your public URL:
   https://house-price-predictor.onrender.com

====================================
✅ VERIFY DEPLOYMENT
====================================

After deployment, test these URLs:

1. Main Interface:
   https://house-price-predictor.onrender.com
   (Should show Gradio UI)

2. API Documentation:
   https://house-price-predictor.onrender.com/docs
   (Should show FastAPI docs)

3. Health Check:
   curl https://house-price-predictor.onrender.com/health
   (Should return: {"status":"healthy","model":"loaded"})

4. Make a Prediction:
   curl -X POST https://house-price-predictor.onrender.com/predict \
     -H "Content-Type: application/json" \
     -d '{"data": [8.3252, 41.0, 6.98, 1.02, 322.0, 2.55, 37.88, -122.23]}'

====================================
⚠️ IMPORTANT NOTES
====================================

Python Version:
  ✓ Set to 3.11 (compatible with all packages)
  ✓ Can change in render.yaml if needed

Free Tier Limits:
  ✓ 750 hours/month (always available)
  ✓ May sleep after 15 min of inactivity
  ✓ Wakes automatically on request
  ✓ Perfect for learning and demos

Dependencies:
  ✓ All specified in requirements.txt
  ✓ Build command installs them all
  ✓ No manual installation needed

Model File:
  ✓ house_model.pkl (681 bytes) is included
  ✓ Loads automatically when app starts
  ✓ No external storage needed

Data Persistence:
  ✓ prediction_history.json saves to app filesystem
  ✓ Clears when app redeploys
  ✓ For persistent storage, upgrade to paid tier

====================================
🔄 REDEPLOYMENT
====================================

To redeploy your app:

Option 1: Automatic (Recommended)
  $ git push -u origin main
  ✓ Render detects change automatically
  ✓ Triggers build & deploy
  ✓ Takes 2-3 minutes

Option 2: Manual via Render Dashboard
  1. Go to your service in Render
  2. Click "Deploy latest commit"
  3. Watch the logs
  4. App redeploys in 2-3 minutes

====================================
📝 CHECKING DEPLOYMENT LOGS
====================================

To see build and start logs:

1. Go to https://dashboard.render.com
2. Select "house-price-predictor"
3. Click "Logs" tab
4. View build and runtime logs
5. Troubleshoot errors if any

Common Log Entries:
  ✓ "Running build command: pip install -r requirements.txt"
  ✓ "Build succeeded"
  ✓ "Running start command: python main.py"
  ✓ "Starting server process"
  ✓ "Application startup complete"

====================================
🆘 TROUBLESHOOTING
====================================

Issue: Build fails
  Fix: Check requirements.txt has correct package names
  Check: pip install -r requirements.txt works locally

Issue: App crashes after build
  Fix: Check main.py has no syntax errors
  Test: python main.py works locally
  Check: All imports are in requirements.txt

Issue: "Module not found" error
  Fix: Add missing module to requirements.txt
  Redeploy: git push -u origin main

Issue: "Port already in use"
  Note: Shouldn't happen on Render (they handle ports)
  If it does: Render automatically assigns free ports

Issue: Model file not found
  Fix: Ensure house_model.pkl in root directory
  Redeploy: git push -u origin main

Issue: App sleeping/slow first request
  Note: Normal on free tier (wakes from sleep)
  Next requests: Much faster

====================================
✨ FINAL RENDER CONFIGURATION
====================================

Your render.yaml is now set up with:

✅ Service name: house-price-predictor
✅ Runtime: Python
✅ Build command: pip install -r requirements.txt
✅ Start command: python main.py
✅ Plan: Free tier
✅ Python version: 3.11

Everything is configured correctly!

====================================
🎯 NEXT: DEPLOY ON RENDER
====================================

Ready to deploy? Follow these steps:

1. Push to GitHub:
   $ git push -u origin main

2. Go to https://render.com

3. Create new Web Service:
   - Connect GitHub
   - Select house-price-api repo
   - Use your render.yaml (auto-detected!)
   - Click Deploy

4. Wait 2-3 minutes for build & deploy

5. Access your app:
   https://house-price-predictor.onrender.com

Done! Your app is live! 🚀

====================================
